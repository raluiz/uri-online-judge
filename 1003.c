#include <stdio.h>
#include <stdlib.h>

int main() {
	//Create two integers.
	int a, b;
	
	//Read a value for each variable.
	scanf("%d %d", &a, &b);
	
	//Print SOMA = a + b.
	printf("SOMA = %d\n", a + b);
	
	return 0;
}
